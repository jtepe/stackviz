version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule: { interval: weekly }
    cooldown:
      default-days: 7
      semver-major-days: 30
    groups:
      dev-tooling:
        dependency-type: development
